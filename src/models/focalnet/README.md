# FocalNet models

Target home for FocalNet model aliases or wrappers.

Current FocalNet support is notebook-first and routed through generic timm helpers
in `src/models/timm_classifier.py`. Move only after the notebook workflow has a
stable shared API.
