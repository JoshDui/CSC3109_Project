# Semantic-guided evaluation

Target home for Semantic-Guided CG-AF CNN evaluation helpers that are currently
implemented as CLI tools under `tools/`.

Migration should happen after root-level `tools/*semantic_guided*.py` wrappers
are in place.
