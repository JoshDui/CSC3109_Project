# Semantic-Guided CG-AF CNN artifacts

Curated artifact bundles for the `Semantic-Guided CG-AF CNN` model.

The current final model artifacts are tracked under `model/semantic_guided_cgaf_*`
with Git LFS. Future cleanup can move or mirror selected final tables, figures,
inputs, and optional ONNX models into date/versioned bundles here once notebook
path discovery supports the bundle layout.
